# S5 `kb-learning` — J.11 manual real-DB smoke transcript

> Generated by `node scripts/qa/manual-smoke-kb-learning.mjs` (rerunnable) on 07.07.2026, 17:06:01, Europe/Kyiv.
>
> Runs entirely against a throwaway temp SQLite file and a throwaway temp COPY of
> `knowledge/school.md` (via `KAMERTON_DB_PATH`/`KAMERTON_KB_PATH`) — the committed
> `kamerton.db`/`knowledge/school.md` are never written. Documented deviations from the literal
> J.11 checklist text are in this script's own header comment
> (`scripts/qa/manual-smoke-kb-learning.mjs`) — summarized: this is the AUTONOMOUS subset
> (`FakeTelegramTransport` + a scripted `FakeModelPort` stand in for the live Telegram chat + live
> claude-sonnet-5 round trip, exactly as `tests/integration/kb-learning/full-flow.test.ts` already
> does); see the "HUMAN LIVE-TELEGRAM STEPS" section below for what still needs a human before archive.


```
=== J.11 kb-learning manual real-DB smoke (scripted) ===
Deviations from the literal J.11 checklist text are documented in this script's own header comment (scripts/qa/manual-smoke-kb-learning.mjs) — summarized: this is the AUTONOMOUS subset (FakeTelegramTransport + a scripted FakeModelPort stand in for the live Telegram chat + live claude-sonnet-5 round trip); the live-Telegram/live-model halves are HUMAN-REQUIRED, listed at the end of this run and in docs/qa/kb-learning-manual-smoke.md's own HUMAN section.

--- 1: from a clean on-disk SQLite file, run the schema; confirm `questions` via PRAGMA table_info ---
  [PASS] temp SQLite file does not exist yet (genuinely clean) — /var/folders/dj/4x31nns17vd3trjsv8vxqf3h0000gn/T/kamerton-smoke-kb-learning-12epkl/smoke.db
  [PASS] questions table exists after initSchema() — bookings,sqlite_sequence,leads,requests,notifications,questions
  [PASS] questions table has every expected column (id, lead_id, request_id, telegram_chat_id, text, answer_source, status, admin_answer, answered_at, delivery_status, created_at) — id,lead_id,request_id,telegram_chat_id,text,answer_source,status,admin_answer,answered_at,delivery_status,created_at

--- preflight: a REAL tmp copy of knowledge/school.md (never the committed file) ---
  [PASS] tmp KB fixture seeded with a COPY of the real repo-root knowledge/school.md

--- 2: a KB-covered question drives a real pipeline turn (@trace FR-FAQ-01, FR-KB-01) ---
  [PASS] the lead's chat received a grounded reply containing the model's KB-grounded narration — ["Зауважте: ваші повідомлення тут обробляє асистент на основі штучного інтелекту Anthropic Claude.\n\nІндивідуальне заняття триває 45 хвилин."]
  [PASS] a questions row was inserted for the covered question
  [PASS] that row's answer_source is 'kb' — kb
  [PASS] the covered-question row does NOT appear in the open inbox

--- 3: an UNCOVERED question drives a log_question row (@trace FR-FAQ-02, FR-KB-01, FR-KB-02) ---
  [PASS] a questions row was inserted for the uncovered question
  [PASS] that row's answer_source is 'unanswered' — unanswered
  [PASS] that row's status is 'open' — open
  [PASS] the uncovered-question row DOES appear in findOpenInboxQuestions (the dashboard inbox's own data source)

--- 4: the admin answers via the REAL answer route (@trace FR-KB-03) ---
  VERBATIM answer response: HTTP 200 {"status":"applied","question":{"id":2,"lead_id":1,"request_id":1,"telegram_chat_id":"smoke-kb-chat-a","text":"Чи є у вас парковка?","answer_source":"unanswered","status":"answered","admin_answer":"Так, біля входу є невеликий безкоштовний паркувальний майданчик.","answered_at":"2026-07-07T14:06:01.217Z","delivery_status":"pending","created_at":"2026-07-07T14:06:01.205Z"}}
  [PASS] HTTP 200 {status:'applied'}
  [PASS] the tmp knowledge/school.md file was appended with EXACTLY the serialized entry, prior content preserved — {"appended":"\n## Чи є у вас парковка?\n\nТак, біля входу є невеликий безкоштовний паркувальний майданчик.\n"}
  [PASS] the row is now status='answered' — answered
  [PASS] delivery_status is still 'pending' (the drain loop, not this route, delivers it) — pending
  [PASS] admin_answer was persisted verbatim
  [PASS] answered_at was stamped (not null)

--- 5: drainQuestionDeliveries delivers it (@trace FR-KB-04) ---
  drain result: {"delivered":1,"failed":0}
  [PASS] first drain: delivered=1, failed=0 — {"delivered":1,"failed":0}
  [PASS] the row's delivery_status is now 'delivered'
  [PASS] the delivered row leaves the open-inbox query's result
  [PASS] FakeTelegramTransport received EXACTLY ONE message to the originating chat with the admin's answer verbatim — [{"kind":"sendMessage","chatId":"smoke-kb-chat-a","text":"Так, біля входу є невеликий безкоштовний паркувальний майданчик."}]

--- 6: a NEW lead asks the SAME previously-uncovered question in a FRESH conversation -> answered from the KB now, fresh-per-turn read, no bot restart (@trace FR-KB-03) ---
  [PASS] the built system prompt for THIS turn contains the exact entry step 4 appended (proves a fresh, per-turn KAMERTON_KB_PATH re-read, not a stale in-memory copy)
  [PASS] the new lead's row now has answer_source='kb' — kb
  [PASS] the new lead's row is a genuinely different lead_id
  [PASS] lead B's chat received a reply containing the KB-sourced answer

--- 7a: a delivery FAILURE on a DIFFERENT answered row -> failed, stays retry-eligible (@trace FR-KB-04, NFR-REL-01) ---
  [PASS] a questions row was inserted for the discount question
  [PASS] the discount question was answered via the route (HTTP 200 {status:'applied'})
  [PASS] the discount row is answered + pending before the forced failure
  drain result (forced failure): {"delivered":0,"failed":1}
  [PASS] second drain: delivered=0, failed=1 (the forced send failure) — {"delivered":0,"failed":1}
  [PASS] the row's delivery_status is now 'failed'
  [PASS] the failed row STAYS in the open-inbox query's result (still needs admin attention/retry)

--- 7b: the retry route flips it back to pending -> the next drain tick delivers it (@trace FR-KB-04) ---
  VERBATIM retry response: HTTP 200 {"status":"applied","question":{"id":4,"lead_id":1,"request_id":1,"telegram_chat_id":"smoke-kb-chat-a","text":"Чи є знижка для двох дітей?","answer_source":"unanswered","status":"answered","admin_answer":"На жаль, знижок за кількість дітей наразі немає.","answered_at":"2026-07-07T14:06:01.219Z","delivery_status":"pending","created_at":"2026-07-07T14:06:01.218Z"}}
  [PASS] HTTP 200 {status:'applied'}
  [PASS] the row's delivery_status is now 'pending' again
  drain result (post-retry): {"delivered":1,"failed":0}
  [PASS] third drain: delivered=1, failed=0 — {"delivered":1,"failed":0}
  [PASS] the row's delivery_status is now 'delivered'
  [PASS] the delivered row leaves the open-inbox query's result
  [PASS] FakeTelegramTransport recorded exactly the 2 expected attempts for the discount answer (1 forced failure from 7a + 1 successful redelivery here) — no further, unexpected attempt — [{"kind":"sendMessage","chatId":"smoke-kb-chat-a","text":"На жаль, знижок за кількість дітей наразі немає."},{"kind":"sendMessage","chatId":"smoke-kb-chat-a","text":"На жаль, знижок за кількість дітей наразі немає."}]

--- 8a: an EMPTY answer and an OVERSIZED (3,501-char) answer -> inline errors, nothing mutated (@trace FR-KB-03) ---
  VERBATIM empty-answer response: HTTP 400 {"status":"invalid","code":"EMPTY","message":"Введіть відповідь."}
  [PASS] an empty (whitespace-only) answer -> inline {status:'invalid', code:'EMPTY'} (never a raw 500) — {"status":"invalid","code":"EMPTY","message":"Введіть відповідь."}
  [PASS] the empty-case row is still 'open' (untouched)
  [PASS] the KB file was NOT written for the empty-answer case
  VERBATIM oversized-answer response: HTTP 400 {status:"invalid",code:"TOO_LONG",maxLength:3500}
  [PASS] a 3,501-character answer -> inline {status:'invalid', code:'TOO_LONG', maxLength:3500} (never a raw 500) — {"status":"invalid","code":"TOO_LONG","message":"Відповідь занадто довга — максимум 3500 символів.","maxLength":3500}
  [PASS] the oversized-case row is still 'open' (untouched)
  [PASS] the KB file was NOT written for the oversized-answer case

--- 8b: a STALE re-submit on an ALREADY-ANSWERED row is a no-op (@trace FR-KB-03) ---
  VERBATIM stale-resubmit response: HTTP 200 {"status":"stale","message":"Це питання вже опрацьоване або більше не існує."}
  [PASS] stale resubmit -> {status:'stale'} (never a raw 500, never re-applies) — {"status":"stale","message":"Це питання вже опрацьоване або більше не існує."}
  [PASS] the KB file is byte-identical before/after the stale resubmit (no second append)
  [PASS] the row's status/admin_answer/answered_at/delivery_status are all UNCHANGED by the stale resubmit — {"before":{"id":2,"lead_id":1,"request_id":1,"telegram_chat_id":"smoke-kb-chat-a","text":"Чи є у вас парковка?","answer_source":"unanswered","status":"answered","admin_answer":"Так, біля входу є невеликий безкоштовний паркувальний майданчик.","answered_at":"2026-07-07T14:06:01.217Z","delivery_status":"delivered","created_at":"2026-07-07T14:06:01.205Z"},"after":{"id":2,"lead_id":1,"request_id":1,"telegram_chat_id":"smoke-kb-chat-a","text":"Чи є у вас парковка?","answer_source":"unanswered","status":"answered","admin_answer":"Так, біля входу є невеликий безкоштовний паркувальний майданчик.","answered_at":"2026-07-07T14:06:01.217Z","delivery_status":"delivered","created_at":"2026-07-07T14:06:01.205Z"}}
  [PASS] no second Telegram message was sent for the stale-resubmitted question (delivery_status stayed 'delivered', never re-queued) — [{"kind":"sendMessage","chatId":"smoke-kb-chat-a","text":"Так, біля входу є невеликий безкоштовний паркувальний майданчик."}]
  [PASS] a further drain tick after the stale resubmit delivers nothing new for this row — {"delivered":0,"failed":0}

=== J.11 SMOKE PASSED (all checks) ===

--- cleanup: close + remove the temp DB/KB dir ---
  temp work dir removed
  [PASS] the committed knowledge/school.md was NEVER written by this run (only its tmp copy was)

--- HUMAN-REQUIRED (not run here — see docs/qa/kb-learning-manual-smoke.md) ---
  (a) Start the real bot + dashboard against .env; as a REAL lead, ask a KB-covered question in a live Telegram chat -> observe a grounded Ukrainian reply arrive, driven by the LIVE claude-sonnet-5 model (this script only proves the wiring with a scripted FakeModelPort).
  (b) Ask an UNCOVERED question -> observe the "адміністраторка уточнить" reply in Telegram AND the row appearing on the REAL rendered dashboard Question-inbox panel (this script only asserts the underlying DB query, not the rendered React panel — already covered by QuestionInbox.test.tsx + the G-stage a11y/vision gate).
  (c) Answer it on the real dashboard -> within a few seconds (the drain tick), observe the admin's answer arrive in the lead's REAL Telegram chat.
  (d) From a DIFFERENT chat, ask the SAME question -> observe it answered from the KB now, with no bot restart.
  (e) Verify no "Using superpowers…"/skill-preamble leak appears in any real reply (the isolation fix) across every step above.
```

## HUMAN LIVE-TELEGRAM STEPS (to run before archive)

The autonomous subset above proves the on-disk, cross-process wiring (real SQLite file, real tmp
KB file, real pipeline/route/drain code paths) with a scripted `FakeModelPort`/`FakeTelegramTransport`
standing in for the live Telegram chat and the live claude-sonnet-5 round trip. Before archiving
`kb-learning`, a human must run the following against the REAL bot, REAL dashboard, a REAL Telegram
test chat, and the LIVE `claude-sonnet-5` model (per `.env`):

- [ ] (a) Start the real bot process AND the real dashboard dev server against `.env` (real
      `TELEGRAM_BOT_TOKEN`, real `KAMERTON_DB_PATH`/default `kamerton.db`, real
      `knowledge/school.md`).
- [ ] (b) As a real lead, in a real Telegram chat, ask a KB-covered question (from the seeded
      `knowledge/school.md` — e.g. "Скільки триває індивідуальне заняття?") -> observe a grounded
      Ukrainian reply arrive in Telegram, driven by the live model.
- [ ] (c) Ask an uncovered question (e.g. something not in `knowledge/school.md`) -> observe the
      "адміністраторка уточнить" reply in Telegram AND the row appearing on the real dashboard's
      Question-inbox panel.
- [ ] (d) Answer it on the real dashboard -> within a few seconds (the drain tick), observe the
      admin's answer arrive in the lead's REAL Telegram chat.
- [ ] (e) From a DIFFERENT chat (a different Telegram account/test chat), ask the SAME
      previously-uncovered question -> observe it answered straight from the KB now
      (`answer_source='kb'`), with NO bot restart between (d) and this step.
- [ ] (f) Verify no "Using superpowers…"/skill-preamble leak appears in ANY real reply across (b)-(e)
      (the isolation fix).

Confirm each box above, then record the outcome here (developer sign-off, date) before this task
(J.11) and the surrounding J.12/J.13 archive steps are considered satisfied.

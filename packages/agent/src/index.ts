// @kamerton/agent — thin, visible tool-use loop on claude-sonnet-5
// (TC-STACK-02). Auth: local user token only — never an API key (NFR-SEC-01).
// The tool set will NEVER include confirm-booking or KB-write operations
// (FR-GUARD-01, FR-GUARD-06) — enforced by static eval asserts.

export {};

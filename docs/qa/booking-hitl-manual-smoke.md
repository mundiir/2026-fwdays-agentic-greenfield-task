# S4 `booking-hitl` — G.3 manual real-DB + real-DEMO-calendar smoke transcript

> Generated by `node scripts/qa/manual-smoke-booking-hitl.mjs` (rerunnable) on 07.07.2026, 17:06:59, Europe/Kyiv.
>
> Hits the REAL DEMO Google Calendar (create/upgrade/delete events, every event tagged
> `[KAMERTON-SMOKE]` and deleted in this script's own `finally` block) and a throwaway temp SQLite
> file (never the real `kamerton.db`). Documented deviations from the literal G.3 checklist text
> are in this script's own header comment (`scripts/qa/manual-smoke-booking-hitl.mjs`) — summarized:
> this is the AUTONOMOUS subset (no live Telegram chat); see the "HUMAN-REQUIRED" section below for
> what still needs a human.

## History: the Confirm exact-overlap double-booking bug (step 8)

1. **REAL BUG FOUND (live)** — an earlier run of this script (commit 5fd1765) found that Google's
   `freebusy.query` MERGES overlapping busy periods from distinct events into one interval, so a
   distinct external event with the exact same `[start,end)` as a booking's own tentative hold was
   silently mistaken for "just my own hold" — Confirm reported `applied` and would have double-booked
   a real external event.
2. **FIX ATTEMPTED (commit 73b4aa7)** — an identity-based collision re-check
   (`CalendarPort.busyEventsInRange`, real `events.list`, excluding the booking's own
   `calendar_event_id` by identity rather than by value) was added alongside the original value-based
   `freeBusy` check.
3. **LIVE-VERIFY RESULT (this run, step 8, PERMANENT/gating)**: **HOLDS UP** — Confirm correctly returned `{status:"conflict"}` for a distinct exact-overlap external event; the identity-based fix is LIVE-VERIFIED against the real DEMO calendar.

```
=== G.3 booking-hitl manual real-DB + real-DEMO-calendar smoke (scripted) ===
Deviations from the literal G.3 checklist text are documented in this script's own header comment (scripts/qa/manual-smoke-booking-hitl.mjs) — summarized: this is the AUTONOMOUS subset (no live Telegram chat); the lead-side pipeline plumbing (free-text propose_slots/ request_hold, the "slot:<n>" callback tap) is already covered by pipeline.test.ts (C.5) and the G.1 integration suite against a FakeCalendarPort — this script's value-add is exercising the admin decision route + outbox drain against the REAL DEMO calendar and a real on-disk DB.

--- preflight: env + real calendar reachability ---
  [PASS] TELEGRAM_BOT_TOKEN set
  [PASS] GOOGLE_APPLICATION_CREDENTIALS set + file exists
  [PASS] GOOGLE_CALENDAR_ID set
  [PASS] an auth signal is present (GOOGLE_APPLICATION_CREDENTIALS) — service-account JSON key path
  [PASS] DEMO calendar reachable (freebusy.query succeeds — calendar is shared with the service account)

--- preflight: temp SQLite + schema ---
  [PASS] notifications table exists after initSchema() — bookings,sqlite_sequence,leads,requests,notifications,questions
  [PASS] requests.offered_slots column exists — id,lead_id,telegram_chat_id,state,student_name,student_age,format,goal_tag,goal_text,tastes,dream_song,experience,comfort,preferred_weekdays,preferred_time_range,created_at,offered_slots
  [PASS] notifications.delivery_status column present

--- preflight: install the REAL GoogleCalendarPort for the decision route ---
  [PASS] real GoogleCalendarPort installed via setCalendarPortForTesting
  target Monday (3 weeks out): 2026-08-03

--- 1: seed a real free on-grid slot + a real tentative event + a pending booking (CONFIRM scenario) ---
  [PASS] real tentative event created for the confirm scenario — idu27ebe91fu650mtdbo5di4pc
  [PASS] pending booking row seeded with request_id + calendar_event_id

--- 2: CONFIRM via the real decision route — the empirical self-collision fix check ---
  VERBATIM confirm response: HTTP 200 {"status":"applied"}
  [PASS] HTTP 200 — 200
  [PASS] response status is 'applied' (NOT 'conflict' — the self-collision instant-compare fix, finding #1) — got status=applied
  [PASS] the real event still exists after Confirm
  [PASS] the real event's status is 'confirmed' (no longer tentative) — status=confirmed
  [PASS] the confirmed event's description contains the first-lesson brief (student name) — "Учень/учениця: KAMERTON-SMOKE Confirm Учень\nВік: 12\nФормат: індивідуальні\nМета занять: для задоволення\nМузичні смаки: лід не назвав музичні смаки\nДосвід: —\nКомфорт зі співом: —\nБажані дні: будь-який день\nБажаний час: вдень"
  [PASS] bookings.status = confirmed — confirmed
  [PASS] a notifications row kind=confirmed delivery_status=pending was inserted — [{"id":1,"booking_id":1,"telegram_chat_id":"smoke-hitl-confirm-chat","kind":"confirmed","payload":"{\"text\":\"Гарні новини: ваше заняття підтверджено на 03.08.2026 о 10:00. До зустрічі 🎵\"}","delivery_status":"pending","created_at":"2026-07-07T14:06:45.940Z","delivered_at":null}]

--- 3: seed a second pending booking + real tentative event (DECLINE scenario) ---
  [PASS] real tentative event created for the decline scenario

--- 4: DECLINE via the real decision route ---
  VERBATIM decline response: HTTP 200 {"status":"applied"}
  [PASS] HTTP 200 {status:'applied'} — {"status":"applied"}
  [PASS] the real tentative event is DELETED (getEvent -> not-found/cancelled) — {"kind":"calendar#event","etag":"\"3566866415644510\"","id":"opd49lcov8jj2k8rrefbl5am4c","status":"cancelled","htmlLink":"https://www.google.com/calendar/event?eid=b3BkNDlsY292OGpqMms4cnJlZmJsNWFtNGMgNWIwM2E1YzEwNmJjZTc1MjQwNWEzMzYzY2MyMjU5MWVhNmFjNzA5ZWFjZTQ3NDZkNzA3YjI2ZjhhNTk3NjcwNEBn","created":"2026-07-07T14:06:46.000Z","updated":"2026-07-07T14:06:47.822Z","summary":"[KAMERTON-SMOKE] decline test 12:00-13:00","description":"scripts/qa/manual-smoke-booking-hitl.mjs — G.3 autonomous smoke","creator":{"email":"fwdays-2026-crash-course@fwdays-2026-crash-course.iam.gserviceaccount.com"},"organizer":{"email":"5b03a5c106bce752405a3363cc22591ea6ac709eace4746d707b26f8a5976704@group.calendar.google.com","displayName":"FWDays DEMO","self":true},"start":{"dateTime":"2026-08-03T12:00:00+03:00","timeZone":"UTC"},"end":{"dateTime":"2026-08-03T13:00:00+03:00","timeZone":"UTC"},"iCalUID":"opd49lcov8jj2k8rrefbl5am4c@google.com","sequence":2,"reminders":{"useDefault":true},"eventType":"default"}
  [PASS] bookings.status = declined — declined
  [PASS] a notifications row kind=declined was inserted — [{"id":2,"booking_id":2,"telegram_chat_id":"smoke-hitl-decline-chat","kind":"declined","payload":"{\"text\":\"На жаль, цей час нам не підходить. Будемо раді бачити вас знову — напишіть нам, коли буде зручно, і ми підберемо інший варіант.\"}","delivery_status":"pending","created_at":"2026-07-07T14:06:47.913Z","delivered_at":null}]

--- 5: seed a third pending booking + real tentative event (PROPOSE ANOTHER TIME scenario) ---
  [PASS] real tentative event created for the propose-another-time scenario

--- 6: PROPOSE ANOTHER TIME via the real decision route (new slot 15:00-16:00) ---
  VERBATIM propose_another_time response: HTTP 200 {"status":"applied"}
  [PASS] HTTP 200 {status:'applied'} — {"status":"applied"}
  [PASS] the OLD tentative event is deleted — {"kind":"calendar#event","etag":"\"3566866420408734\"","id":"lfq4pn25u2ef2532emk46p1aus","status":"cancelled","htmlLink":"https://www.google.com/calendar/event?eid=bGZxNHBuMjV1MmVmMjUzMmVtazQ2cDFhdXMgNWIwM2E1YzEwNmJjZTc1MjQwNWEzMzYzY2MyMjU5MWVhNmFjNzA5ZWFjZTQ3NDZkNzA3YjI2ZjhhNTk3NjcwNEBn","created":"2026-07-07T14:06:48.000Z","updated":"2026-07-07T14:06:50.204Z","summary":"[KAMERTON-SMOKE] propose-another-time old hold 14:00-15:00","description":"scripts/qa/manual-smoke-booking-hitl.mjs — G.3 autonomous smoke","creator":{"email":"fwdays-2026-crash-course@fwdays-2026-crash-course.iam.gserviceaccount.com"},"organizer":{"email":"5b03a5c106bce752405a3363cc22591ea6ac709eace4746d707b26f8a5976704@group.calendar.google.com","displayName":"FWDays DEMO","self":true},"start":{"dateTime":"2026-08-03T14:00:00+03:00","timeZone":"UTC"},"end":{"dateTime":"2026-08-03T15:00:00+03:00","timeZone":"UTC"},"iCalUID":"lfq4pn25u2ef2532emk46p1aus@google.com","sequence":2,"reminders":{"useDefault":true},"eventType":"default"}
  [PASS] bookings.status = cancelled (superseded) — cancelled
  [PASS] requests.state = proposing — proposing
  [PASS] requests.offered_slots persisted with the admin's validated slot — [{"start":"2026-08-03T15:00","end":"2026-08-03T16:00"}]
  [PASS] a notifications row kind=proposed_again with a buttons payload was inserted — [{"id":3,"booking_id":3,"telegram_chat_id":"smoke-hitl-propose-chat","kind":"proposed_again","payload":"{\"text\":\"На жаль, попередній час не підійшов, тож ми пропонуємо інший: 03.08.2026 о 15:00. Оберіть зручний варіант, і ми запишемо вас.\",\"buttons\":[[{\"text\":\"03.08 о 15:00\",\"data\":\"slot:0\"}]]}","delivery_status":"pending","created_at":"2026-07-07T14:06:50.307Z","delivered_at":null}]

--- 7: OUTBOX DRAIN — a FAKE Telegram transport (real send needs a live chat, see HUMAN-REQUIRED) ---
  drain result: {"delivered":3,"failed":0}
  [PASS] drain delivered exactly the 3 pending rows seeded above — {"delivered":3,"failed":0}
  [PASS] the fake transport received 3 sendMessage calls, in FIFO (id ASC) order — ["sendMessage","sendMessage","sendMessage"]
  [PASS] the confirm chat received the exact confirmation text — "Гарні новини: ваше заняття підтверджено на 03.08.2026 о 10:00. До зустрічі 🎵"
  [PASS] the decline chat received the exact decline text (kind, door-open copy) — "На жаль, цей час нам не підходить. Будемо раді бачити вас знову — напишіть нам, коли буде зручно, і ми підберемо інший варіант."
  [PASS] the propose-another-time chat received the re-proposal text WITH real slot-chip buttons — {"buttons":[[{"text":"03.08 о 15:00","data":"slot:0"}]]}
  [PASS] all 3 notifications rows are now delivered — ["delivered","delivered","delivered"]
  [PASS] second drain call: 0 delivered, 0 failed (nothing left to send) — {"delivered":0,"failed":0}

--- 8: CONFIRM exact-overlap collision regression (PERMANENT, gating) — the live-found Confirm double-booking bug, FIXED in commit 73b4aa7 by an identity-based collision re-check (CalendarPort.busyEventsInRange, google-calendar.ts) run ALONGSIDE the pre-existing value-based freeBusy check in route.ts's Confirm step 4. Empirical, real-Google-Calendar-API-only proof: a DISTINCT external event (B) with the EXACT SAME [start,end) as the booking's own tentative hold (A) must still be detected as a genuine collision, immune to freebusy.query's own merging of overlapping busy periods from different events into one interval (FakeCalendarPort's freeBusy never merges, so the unit/integration suite structurally cannot exercise this path). ---
  [PASS] event A (the booking's own real tentative hold) created for the exact-overlap regression — 5smq8pkieo4h9guuba4im8p5ro
  [PASS] event A and event B are DISTINCT ids (never the same event) — A=5smq8pkieo4h9guuba4im8p5ro B=c95b64jjjmpqlo24d5dgip7odg
  VERBATIM exact-overlap confirm response: HTTP 200 {"status":"conflict","message":"Цей час більше не вільний у календарі — оновіть сторінку і оберіть інший варіант."}
  [PASS] Confirm against event B (exact-same-range external event) surfaces {status:'conflict'} (NOT 'applied') — the identity-based fix (commit 73b4aa7) correctly distinguishes A from B by eventId, immune to freebusy.query's merging — {"status":"conflict","message":"Цей час більше не вільний у календарі — оновіть сторінку і оберіть інший варіант."}
  [PASS] booking stays pending after the exact-overlap conflict (no confirmed event, nothing sent) — pending
  [PASS] no notification row inserted on the exact-overlap conflict — []
  [PASS] event A (the booking's own hold) is still tentative — NOT upgraded to confirmed — "tentative"
  LIVE-VERIFIED: the exact-overlap Confirm regression correctly returns {status:'conflict'} against the REAL DEMO calendar — commit 73b4aa7's identity-based busyEventsInRange fix holds up live (Google's freebusy.query still merges the two events into one interval; events.list/busyEventsInRange does not, and the identity exclusion by eventId is what tells A and B apart).

--- 9: PROPOSE ANOTHER TIME with a Saturday (off-grid) slot — inline OFF_GRID validation, nothing sent ---
  VERBATIM off-grid response: HTTP 200 {"status":"invalid","code":"OFF_GRID","message":"Обраний час поза графіком занять (Пн–Пт, до 19:00).","slot":{"start":"2026-07-11T12:00","end":"2026-07-11T13:00"}}
  [PASS] off-grid Saturday slot -> {status:'invalid', code:'OFF_GRID'} — {"status":"invalid","code":"OFF_GRID","message":"Обраний час поза графіком занять (Пн–Пт, до 19:00).","slot":{"start":"2026-07-11T12:00","end":"2026-07-11T13:00"}}
  [PASS] the original tentative event is UNTOUCHED (still exists, not confirmed/deleted) — "tentative"
  [PASS] no notification row inserted on an invalid propose_another_time — []

--- note: NFR-REL-01 calendar-failure branches (D.4/D.6) are covered by unit tests, not repeated here ---
  Deliberately not reproduced against the REAL calendar (would require killing/blackholing a live network call mid-run to force a CalendarError, which is not cheaply/safely scriptable without leaving calendar cruft on a genuine timeout) — already covered by apps/dashboard/app/api/decisions/[requestId]/route.test.ts's D.4/D.6 suites against a FakeCalendarPort configured to throw. This matches the task instructions' own "only if you can do it without leaving calendar cruft; otherwise note it's covered by the unit tests" guidance.

=== G.3 SMOKE (autonomous subset) PASSED ===

--- cleanup: delete EVERY tracked real calendar event, close + remove the temp DB ---
  cleaned up 4 tracked event(s)
  temp db removed

--- HUMAN-REQUIRED (not run here) ---
  (a) Walk a REAL lead through Telegram intake to `proposing` and tap a real inline slot chip — creates the pending booking from the lead side (the model calling propose_slots/request_hold over a live Anthropic round trip, and grammY delivering a real callback tap). Already covered for the STATE-MACHINE/DB mechanics by pipeline.test.ts + the G.1 integration suite; this step proves the live Telegram UX end to end.
  (b) Observe the REAL Telegram confirmation/decline/re-proposal messages arriving in a live test chat (this script's own step 7 proved the outbox drain logic + payload shape against a FAKE transport — a real `transport.sendMessage` round trip to Telegram's API is untested here).
  (c) The outbox restart-durability check: kill the bot process mid-decision, make the decision on the dashboard while the bot is down, confirm the notifications row stays `pending` in the SQLite file, restart the bot, confirm the queued message delivers on the next drain tick (design.md Decision 1's durability guarantee — requires a real second OS process, not exercised here).
```

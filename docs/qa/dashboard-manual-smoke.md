# S3 `dashboard` — 8.11 manual real-DB smoke transcript

> Generated by `node scripts/qa/manual-smoke-dashboard.mjs` (rerunnable) on 07.07.2026, 17:06:03, Europe/Kyiv.
>
> Proves the cross-process AG-UI bridge end-to-end: a REAL `next start` dashboard process (built via
> `next build`), a REAL on-disk SQLite file shared with an in-process "bot" driver
> (`packages/bot/src/pipeline.ts`'s `handleUpdate`), a REAL HTTP POST bridge
> (`packages/bot/src/http-agui-publisher.ts`'s `HttpAguiPublisher` -> `/api/agui/ingest`), and a REAL
> Server-Sent Events GET (`/api/agui/stream`) read with a plain `fetch` + `ReadableStream` reader (no
> `EventSource` global in Node). The model is a DETERMINISTIC scripted `FakeModelPort`, and Telegram is
> a `FakeTelegramTransport` — no live Telegram chat or Anthropic call is made (deviations documented in
> this script's own header comment, `scripts/qa/manual-smoke-dashboard.mjs`, along with which parts of
> the literal 8.11 checklist this pass covers vs. defers to `capture-dashboard.mjs`'s browser-driven
> pixel assertions).

```
=== 8.11 manual real-DB smoke (scripted) — S3 dashboard ===
Deviations from the literal 8.11 text are documented in this script's own header comment (scripts/qa/manual-smoke-dashboard.mjs) — summarized: a FakeTelegramTransport + deterministic FakeModelPort stand in for the real Telegram/Claude round trip (already covered by S2's 6.9 smoke); the pending booking seeded below has calendar_event_id=NULL (the calendar-delete-before-DB-delete ordering is already covered by route.test.ts's FakeCalendarPort integration test); the browser/pixel assertions (Confirm stub copy, HallMap click, dev-server-restart reconnect UI) are capture-dashboard.mjs's job, not this script's — this script instead proves the real cross-process HTTP+SSE bytes and the real DB rows on both sides of them.
ENVIRONMENT NOTE: runs under plain `node scripts/qa/manual-smoke-dashboard.mjs` (like the other manual-smoke scripts). An earlier version needed `--experimental-transform-types` because packages/bot/src/http-agui-publisher.ts used a TS constructor-parameter-property strip-only Node rejects — a runtime-only bug this smoke surfaced (it would also have crashed the real bot entrypoint); now fixed (explicit field), so no flag is needed here or for the bot.

--- step 0: build present ---
  [PASS] apps/dashboard/.next exists (next build already run) — /Users/oleksandrkostylev/Workspace/fwdays/2026-fwdays-agentic-greenfield-task/apps/dashboard/.next

--- step 1: temp SQLite file + free port ---
  [PASS] leads/requests/bookings tables created from a clean file — bookings,sqlite_sequence,leads,requests,notifications,questions
  temp db: /var/folders/dj/4x31nns17vd3trjsv8vxqf3h0000gn/T/kamerton-dashboard-smoke-bNPCdK/smoke.db
  dashboard port: 49422

--- step 1b: boot the real dashboard (`next start`) ---
  [PASS] real `next start` process reachable at http://127.0.0.1:49422

--- scenario 1: schema/empty — first SSE frame on a fresh DB ---
  [PASS] first SSE frame is STATE_SNAPSHOT — "STATE_SNAPSHOT"
  [PASS] empty DB -> empty activeRequests — []
  [PASS] empty DB -> empty pendingQueue — []
  [PASS] empty DB -> hallMap still renders the full 50-seat grid (5 weekdays x 10 hours) — 50

--- scenario 2: live turn — bot(in-process) -> HTTP POST -> hub -> SSE ---
  observed SSE event order for this turn: RUN_STARTED -> TEXT_MESSAGE_START -> TEXT_MESSAGE_CONTENT -> TEXT_MESSAGE_END -> STATE_SNAPSHOT -> RUN_FINISHED
  [PASS] exact AG-UI event order: RUN_STARTED -> TEXT_MESSAGE_START -> TEXT_MESSAGE_CONTENT -> TEXT_MESSAGE_END -> STATE_SNAPSHOT -> RUN_FINISHED — RUN_STARTED -> TEXT_MESSAGE_START -> TEXT_MESSAGE_CONTENT -> TEXT_MESSAGE_END -> STATE_SNAPSHOT -> RUN_FINISHED
  [PASS] RUN_STARTED/RUN_FINISHED both carry this turn's threadId (the lead's telegramChatId) — RUN_STARTED.threadId=smoke-dash-chat-1 RUN_FINISHED.threadId=smoke-dash-chat-1
  [PASS] TEXT_MESSAGE_CONTENT carries non-empty streamed reply text — "Зауважте: ваші повідомлення тут обробляє асистент на основі штучного інтелекту Anthropic Claude.\n\nДякую! Скільки років учню чи учениці?"
  [PASS] the turn's STATE_SNAPSHOT carries the saved studentName field (first-turn brand-new-lead snapshot) — {"studentName":"Софійка"}
  [PASS] lead row now exists in the temp DB — {"id":1,"telegram_user_id":"smoke-dash-1","telegram_chat_id":"smoke-dash-chat-1","telegram_display_name":"Софійка (dashboard smoke)","created_at":"2026-07-07T14:06:02.656Z"}
  [PASS] a requests row exists with student_name = 'Софійка' — {"id":1,"lead_id":1,"telegram_chat_id":"smoke-dash-chat-1","state":"qualifying","student_name":"Софійка","student_age":null,"format":null,"goal_tag":null,"goal_text":null,"tastes":null,"dream_song":null,"experience":null,"comfort":null,"preferred_weekdays":null,"preferred_time_range":null,"created_at":"2026-07-07T14:06:02.656Z","offered_slots":null}

--- scenario 3: pending queue appears in a fresh connection's STATE_SNAPSHOT ---
  [PASS] fresh connection's first frame is STATE_SNAPSHOT
  [PASS] pendingQueue now contains this request — [{"requestId":1,"leadId":1,"telegramChatId":"smoke-dash-chat-1","studentName":"Софійка","studentAge":null,"brief":"Учень/учениця: Софійка\nВік: —\nФормат: —\nМета занять: лід не назвав мету занять\nМузичні смаки: лід не назвав музичні смаки\nДосвід: —\nКомфорт зі співом: —\nБажані дні: —\nБажаний час: —","bookingId":1,"calendarEventId":null,"slotStart":"2026-07-07T16:00:00+03:00","slotEnd":"2026-07-07T17:00:00+03:00"}]
  [PASS] pending entry's studentName is 'Софійка' — {"requestId":1,"leadId":1,"telegramChatId":"smoke-dash-chat-1","studentName":"Софійка","studentAge":null,"brief":"Учень/учениця: Софійка\nВік: —\nФормат: —\nМета занять: лід не назвав мету занять\nМузичні смаки: лід не назвав музичні смаки\nДосвід: —\nКомфорт зі співом: —\nБажані дні: —\nБажаний час: —","bookingId":1,"calendarEventId":null,"slotStart":"2026-07-07T16:00:00+03:00","slotEnd":"2026-07-07T17:00:00+03:00"}
  [PASS] pending entry carries a non-empty first-lesson brief — "Учень/учениця: Софійка\nВік: —\nФормат: —\nМета занять: лід не назвав мету занять\nМузичні смаки: лід не назвав музичні смаки\nДосвід: —\nКомфорт зі співом: —\nБажані дні: —\nБажаний час: —"
  [PASS] pending entry references the seeded bookingId — 1 vs 1

--- scenario 4: DELETE /api/leads/:id over real HTTP ---
  [PASS] DELETE responds 200 — 200
  [PASS] DELETE response body is { status: 'ok' } — {"status":"ok"}
  [PASS] leads row is gone from the temp DB
  [PASS] requests row is gone from the temp DB
  [PASS] bookings row is gone from the temp DB
  [PASS] connected SSE client receives a fresh STATE_SNAPSHOT after the delete — ["STATE_SNAPSHOT"]
  [PASS] that snapshot's pendingQueue no longer contains the deleted request — []
  NOTE: calendar_event_id was NULL for this booking (documented deviation, this script's header) — no calendar.deleteEvent() call happens on this path; the calendar-delete-before-DB-delete ordering is covered by route.test.ts's FakeCalendarPort integration test instead.

--- scenario 5: bad input ---
  [PASS] DELETE /api/leads/abc -> 400 — 400
  [PASS] POST /api/agui/ingest {} -> 400 — 400

--- scenario 6: process independence (NFR-LOCAL-01) ---
  the 'bot' in this run was always in-process handleUpdate() calls, not a second OS process (documented deviation, this script's header) — there is no bot process to kill. What IS asserted: the dashboard is its own process, independent of anything else, and keeps serving.
  [PASS] dashboard root route still responds 200 with no bot process involved — 200

--- cleanup ---
  SSE clients closed, dashboard server stopped, temp db removed

=== 8.11 SMOKE PASSED (all checks) ===
```

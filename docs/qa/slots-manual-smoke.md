# S1 `slots` — 6.9 manual real-DB smoke transcript

> Generated by `node scripts/qa/manual-smoke-slots.mjs` (rerunnable) on 2026-07-04, Europe/Kyiv, against the real DEMO Google Calendar + a clean on-disk SQLite file.
> Deviations from the literal 6.9 text: the step-4 busy event is API-created (target = next Monday, not "tomorrow", to stay inside the Mon-Fri grid); UI screenshots deferred to the QA-proof stage.

```
=== 6.9 manual real-DB smoke (scripted) — target Monday 2026-07-06 ===
  (pre-run sweep removed 1 leaked [smoke-6.9] event(s))

--- step 1: env ---
  [PASS] GOOGLE_CALENDAR_ID set
  [PASS] GOOGLE_APPLICATION_CREDENTIALS set + file exists

--- step 2: clean SQLite + schema ---
  [PASS] bookings table created from clean file — bookings,sqlite_sequence

--- step 3: grid, first day ---
  first-day starts: 10:00 11:00 12:00 13:00 14:00 15:00 16:00 17:00 18:00 19:00
  [PASS] 10 slots, 10:00..19:00 hourly

--- step 4: external busy 13:00-14:00 + subtraction ---
  full free pool (grid - busy): 10:00 11:00 12:00 14:00 15:00 16:00 17:00 18:00 19:00
  [PASS] full pool: 9 slots, 13:00 excluded, 12:00+14:00 kept
  ranked top-3 proposals: 10:00 12:00 14:00 (12:00/14:00 hug the busy block, 10:00 hugs the grid edge — teacher compactness)
  [PASS] proposals ≤3, none overlaps busy 13:00
  [PASS] 12:00 and 14:00 proposed (adjacency ranks them top)

--- step 5: hold + pending row ---
  [PASS] hold created — status=held
  [PASS] pending row with calendar_event_id
  (visual UI confirmation + screenshot deferred to QA-proof stage)

--- step 6: collision path ---
  [PASS] second hold collides — status=collision
  [PASS] still exactly one pending row — count=1

--- step 7: cancel path ---
  [PASS] no pending rows after cancel
  [PASS] 12:00 free again after release

--- step 8: outage path (broken key path, .env untouched) ---
  [PASS] calendar_unavailable result
  [PASS] exact Ukrainian apology
  [PASS] no unhandled rejection

--- cleanup ---
  step-4 busy event deleted, temp db removed

=== 6.9 SMOKE PASSED (all checks) ===
```
